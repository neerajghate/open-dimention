{
  "name": "dimention",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "dev": "node server/server.mjs --dev",
    "build": "vite build",
    "start": "node server/server.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "three": "^0.180.0"
  },
  "devDependencies": {
    "vite": "^7.1.5"
  }
}
